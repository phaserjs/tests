// import { GetRandom, Matrix, NumberArray, NumberArrayStep } from '../../../phaser-genesis/src/utils/array';

describe("Array - tests", () =>
{
    // TODO: Create test
    test('GetRandom', () =>
    {
//         const array_test = ["one", "two", "three", "four"];
//         for(let i = 0; i < 20; i++) {
//             const result = GetRandom(array_test, 1);
//             console.log(result)
//             expect(result).toMatch(/two|three|four/);
//         }
    })
});